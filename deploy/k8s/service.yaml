apiVersion: v1
kind: Service
metadata:
  name: multi-ds-manager
spec:
  selector:
    app: multi-ds-manager
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: metrics
      port: 9095
      targetPort: metrics
  type: ClusterIP
